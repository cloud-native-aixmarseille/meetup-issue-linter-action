{
	"name": "csv-referential-repository",
	"projectType": "library",
	"sourceRoot": "packages/adapter/csv-referential-repository/src",
	"targets": {
		"typecheck": {
			"executor": "nx:run-commands",
			"options": {
				"command": "tsc --noEmit -p packages/adapter/csv-referential-repository/tsconfig.lib.json"
			}
		},
		"test": {
			"executor": "nx:run-commands",
			"options": {
				"command": "vitest run packages/adapter/csv-referential-repository"
			}
		}
	},
	"tags": [
		"type:adapter",
		"technology:csv",
		"responsibility:referential-repository"
	]
}
