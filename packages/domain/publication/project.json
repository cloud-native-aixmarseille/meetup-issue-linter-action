{
	"$schema": "../../../node_modules/nx/schemas/project-schema.json",
	"name": "domain-publication",
	"projectType": "library",
	"sourceRoot": "packages/domain/publication/src",
	"tags": ["type:domain", "scope:publication"],
	"targets": {
		"typecheck": {
			"executor": "nx:run-commands",
			"options": {
				"command": "tsc --noEmit -p packages/domain/publication/tsconfig.lib.json"
			}
		},
		"test": {
			"executor": "nx:run-commands",
			"options": {
				"command": "vitest run --config packages/domain/publication/vitest.config.ts"
			}
		}
	}
}
