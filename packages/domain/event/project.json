{
	"$schema": "../../../node_modules/nx/schemas/project-schema.json",
	"name": "domain-event",
	"projectType": "library",
	"sourceRoot": "packages/domain/event/src",
	"tags": ["type:domain", "scope:event"],
	"targets": {
		"typecheck": {
			"executor": "nx:run-commands",
			"options": {
				"command": "tsc --noEmit -p packages/domain/event/tsconfig.lib.json"
			}
		},
		"test": {
			"executor": "nx:run-commands",
			"options": {
				"command": "vitest run --config packages/domain/event/vitest.config.ts"
			}
		}
	}
}
